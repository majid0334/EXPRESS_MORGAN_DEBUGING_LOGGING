import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import { ApolloClient, InMemoryCache } from '@apollo/client'
import {creaeteAppoloProvider} from 
createApp(App).use(router).mount('#app')
